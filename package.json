{
  "private": true,
  "scripts": {
    "postinstall": "(cd packages/nuxt; yarn install); (cd packages/firebase/functions; yarn install); (cd packages/algolia-sync; yarn install)",
    "lint": "eslint '**/*.{ts,vue}'"
  },
  "packageManager": "yarn@4.3.1",
  "devDependencies": {
    "eslint": "^9.2.0"
  }
}
